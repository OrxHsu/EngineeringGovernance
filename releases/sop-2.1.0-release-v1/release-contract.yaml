releaseId: sop-2.1.0-release-v1
packageVersion: 2.1.0
status: published
license: Apache-2.0
remote: origin
defaultBranch: main
scope:
  - source
  - schemas
  - tests
  - Codex Skill adapter
  - reproducible Node 22 runner
  - security policy
nonGoals:
  - npm publication
  - deployment or external consumer migration
acceptance:
  - package and VERSION are exactly 2.1.0
  - two isolated runner builds are byte-identical
  - clean Node 22 pnpm check passes
  - project and pinned runner checks return valid true
  - global Codex adapter check returns valid true
  - repository is public at the recorded release URL
  - tag and GitHub Release are published
nextAction: npm publication remains deferred and requires a separate decision.
